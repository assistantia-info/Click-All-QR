{
  "name": "click-all-qr-e2e",
  "version": "0.2.0",
  "private": true,
  "description": "End-to-end tests for the Click All QR extension (Playwright, real Chromium + --load-extension).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
